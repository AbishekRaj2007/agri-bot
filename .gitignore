# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env

# Server secrets and user data — never commit these
server/.env
server/users.json

# Blockchain — secrets and generated artifacts
# ⚠️  NEVER commit PRIVATE_KEY or any .env file in the blockchain folder
blockchain/**/.env
blockchain/**/artifacts/
blockchain/**/cache/
blockchain/**/typechain-types/
blockchain/**/ignition/deployments/
# Hardhat v3 vars are stored in ~/.hardhat/vars.json (outside repo — always safe)

# Root .env after blockchain deployment (contains VITE_CONTRACT_ADDRESS)
# .env is already listed above — this comment is a reminder


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
